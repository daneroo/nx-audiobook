# dindind - deno-in-docker-in-deno

This is a test of invoking a deno runtime inside a docker container, which is itself invoked by a deno runtime.

What kinds of interprocess communications can we have:

- is stdio workable
- is http better
